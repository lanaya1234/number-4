int* p=&a    生成一个指针p并指向变量a的地址；
 int *p;p=&a;
int** pr=&p   指针的指针；
malloc(size) 是个专门在堆上分配内存的函数；
free(p) 释放内存参数是内存首地址；次数和malloc()函数次数相等；之后把p指针置空；
int a[];a数组首元素的地址；&a是这个数组的地址；a与&a值相同意义不一样；
xx->x:xx为某个结构体的指针，x为结构体的属性；->是获得指针指向的复杂类型目标的成员的操作符；
typedef int size，size a=int a；
